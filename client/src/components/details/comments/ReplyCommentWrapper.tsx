import styled from 'styled-components';

export default function ReplyCommentWrapper() {
  return (
    <ReplyCommentWrapperWrapper>ReplyCommentWrapper</ReplyCommentWrapperWrapper>
  );
}

const ReplyCommentWrapperWrapper = styled.div``;
